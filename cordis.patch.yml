# Optional Cordis patch fragment — not activated on any profile by this spike.
# Operator must authorize profile wiring separately.
#
# Live Gateway smoke (LAB only) requires explicit operator config, e.g.:
#   transport: discordjs
#   allowConnect: true   # NEVER default; safety gate
#   accounts.lab with allowlists (no wildcards) + DISCORD_LAB_BOT_TOKEN in secrets
- insert:
    - id: dsh-piblox-discord
      name: 'dsh-piblox-discord'
      config:
        transport: fake
        allowConnect: false
        accounts: {}
